<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Header</title>
    <link rel="stylesheet" href="{{ asset('css/cabecera.css') }}">
</head>
<body>
    <nav>
      <div class="navbar">
        <div class="container nav-container">
            <input class="checkbox" type="checkbox" name="" id="" />
            <div class="hamburger-lines">
              <span class="line line1"></span>
              <span class="line line2"></span>
              <span class="line line3"></span>
            </div>  
          <div class="logo">
            <h1>Quality</h1>
          </div>
          <div class="menu-items">
            <li><a href="{{path('ctrl_login')}}">Home</a></li>
            <li><a href="{{path('mostrar_inventario')}}">Solicitar material</a></li>
            <li><a href="{{path('mostrar_solicitudes')}}">Mis peticiones</a></li>
            <li><a href="#">Mi material</a></li>
            {% if is_granted('ROLE_ADMIN') %}
            <li><a href="{{path('home_admin')}}">Zona adminstrador</a></li>
            {% endif %}
            <li><a href="{{path('ctrl_logout')}}">Cerrar sesión</a></li>
          </div>
        </div>
      </div>
    </nav>
</body>
</html>